{
  "name": "abi",
  "private": true,
  "version": "0.1.0",
  "description": "ABI - gercek zamanli konusan dijital karakter",
  "workspaces": [
    "web",
    "desktop"
  ],
  "scripts": {
    "dev": "concurrently -n sunucu,arayuz -c cyan,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "python3 scripts/serve.py",
    "dev:web": "npm run dev -w web",
    "setup": "python3 -m venv .venv && .venv/bin/pip install -r server_py/requirements.txt && npm install",
    "voices": "python3 scripts/fetch_models.py",
    "build": "npm run build -w web",
    "start": "python3 scripts/serve.py",
    "typecheck": "npm run typecheck -w web && npm run typecheck -w desktop",
    "desktop": "npm run build && npm run start -w desktop",
    "desktop:dev": "npm run dev -w desktop"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
