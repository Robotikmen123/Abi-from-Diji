{
  "name": "desktop",
  "private": true,
  "version": "0.1.0",
  "description": "ABI masaustu kabugu: overlay, mini mod, her zaman ustte",
  "main": "dist/main.cjs",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/rename.mjs",
    "dev": "npm run build && electron . --dev",
    "start": "npm run build && electron .",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "electron": "^43.4.0",
    "typescript": "^5.7.3"
  }
}
